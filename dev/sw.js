/* ==================================================================
   このファイルは「アプリとして入れられる」ようにするためだけのものです。
   2026-08-04

   EdgeやChromeは、サービスワーカーが無いページを
   「アプリとして入れられる」とみなしません。
   そのため、1押しで入るボタンが一度も出ていませんでした。

   ★中身はいっさい保存しません。通信はそのまま通します。
   　保存すると古い版が端末に残り、版ずれの原因になるためです。
   ================================================================== */
self.addEventListener('install', function () { self.skipWaiting(); });
self.addEventListener('activate', function (e) { e.waitUntil(self.clients.claim()); });
self.addEventListener('fetch', function () { /* 何もしない＝ふつうに通信する */ });
